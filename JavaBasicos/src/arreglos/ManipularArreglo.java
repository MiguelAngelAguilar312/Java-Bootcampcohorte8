/*Crea una clase llamara ManipularArreglo
crea un array con los siguientes números enteros:

1, 6, 45, 53, 80, 102, 145, 326, 450, 892
Crea un programa que use un ciclo que tome los números de arreglo y devuelva la suma total, 
la suma de los números pares y la suma de los números impares.
*/

//package arreglos;

//public class ManipularArreglo {
	//public static void main (String[] args) {
		
		/*
		 * int numeros []; 
		 * numeros = new int [10];
		 * 
		 * numeros[0] = 1; 
		 * numeros[1] = 6; 
		 * numeros[2] = 45; 
		 * numeros[3] = 53; 
		 * numeros[4] = 80; 
		 * numeros[5] = 102; 
		 * numeros[6] = 145; 
		 * numeros[7] = 326; 
		 * numeros[8] = 450;
		 * numeros[9] = 892;
		 * 
		 * int suma = numeros[0] + numeros[1] + numeros[2] + numeros[3] + numeros[4] 
		 *            + numeros[5] +numeros[6] + numeros[7] + numeros[8] + numeros[9]; 
		 * int sumap = numeros[0] + numeros[1] + numeros[4] + numeros[5] + numeros[7] 
		 *             + numeros[8] + numeros[9]; 
		 * int sumai = numeros[0] + numeros[2] + numeros[3] + numeros[6];
		 * System.out.println("La suma de todos los elementos " + suma);
		 * System.out.println("La suma de todos los elementos pares " + sumap);
		 * System.out.println("La suma de todos los elementos inpares " + sumai);
		 */

         /*int numeros[]:
          * numeros = new int[] (1, 6, 45, 53, 80, 102, 145, 326, 450, 892);
          * 
          * int sumaTotal = 0;
          * int sumaPar = 0;
          * int sumaInpar = 0;
          * 
          * for (int i=0; numeros.length; i++) {
          *      if (numeros[i] % 2 ==0) {
          *      sumaPar += numeros[i];
          *      } else {
          *      sumaImpar += numero[i];
          *      }
          *      sumaTotal += numero[i];   
          * }
          *System.out.println("La suma de todos los elementos es: " + sumaTotal);
		  *System.out.println("La suma de todos los elementos pares: " + sumaPar);
		  *System.out.println("La suma de todos los elementos inpares: " + sumaImpar);
          * */
		
		
		
	//}

//}
